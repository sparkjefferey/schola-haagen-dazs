# 沙藏学馆 · 部署到香港/境外服务器（大白话指南）

> 适用对象：不太懂服务器、但想让网站 24 小时稳定对外访问的你。
> 目标：租一台**香港（或境外）轻量服务器** → 用 Docker 一键跑起来 → 不用备案。

---

## 为什么是「香港/境外 + Docker」

- **免备案**：国内服务器绑域名必须 ICP 备案（1~2 周，还要实名、论坛还可能卡专项审批）。香港/新加坡节点**不用备案，开了就能用**。
- **数据不丢**：网站用本地文件数据库（SQLite），必须放在「持久硬盘」上。Docker 容器一重启就还原，所以数据库专门挂在一个「数据卷」里，重启/更新都不丢。
- **省维护**：Docker 把网站、数据库、依赖全打包，进程挂了会自动拉起，你基本不用管。

---

## 第一步：租一台香港轻量服务器

任选一家（新用户都有优惠）：

| 厂商 | 节点举例 | 新用户价位（参考） |
|---|---|---|
| 阿里云国际站 | 香港轻量应用服务器 | 约几美元/月 |
| 腾讯云 | 轻量应用服务器·中国香港 | 常有过百元/年活动 |
| UCloud | 香港云主机 | 按量或包年，常有优惠 |

**选购要点：**
- 系统选 **Ubuntu 22.04 LTS**（最省心，文档多）。
- 配置 **1 核 1G 起步**，预算够就 2G（网站很小，1G 够用）。
- 带宽选**按流量包**或**峰值 3~5Mbps**，小网站足够。
- 拿到两样东西：**公网 IP** 和 **root 密码**（或密钥）。

> 租好后，在厂商控制台确认「**防火墙/安全组**」放开了 **3000 端口**（或你打算用的 80 端口）的入站规则，否则外部访问不进来。

---

## 第二步：登录服务器

在你自己电脑的「终端」（Mac 用自带终端，Windows 用 PowerShell 或终端）里输入：

```bash
ssh root@你的服务器IP
```

第一次会问你 yes/no，输 `yes`，再输密码。看到命令行变成 `root@xxxx` 就是登进去了。

---

## 第三步：把代码弄到服务器

两种方式，选一种：

**方式 A（推荐，最省事）—— git 克隆：**
如果你把代码传到了 GitHub/GitLab，在服务器上：
```bash
git clone 你的仓库地址 schola-haagen-dazs
cd schola-haagen-dazs
```

**方式 B —— 直接传文件夹：**
在你**自己电脑**上，进入项目目录，执行：
```bash
scp -r . root@你的服务器IP:~/schola-haagen-dazs
```
然后回服务器 `cd schola-haagen-dazs`。

---

## 第四步：一键部署

在服务器项目目录里，跑：

```bash
bash deploy.sh
```

脚本会自动：装 docker → 构建镜像 → 启动容器 → 初始化示例数据。
首次跑会联网下载东西，可能要几分钟，等它跑完出现「部署完成 🎉」即可。

---

## 第五步：打开网站

浏览器访问： **`http://你的服务器IP:3000`**

- 第一个注册的人会自动成为**掌门（管理员）**。
- 之后在燕京阁可以给别人「授认证印」、管理论文等。

---

## 常见问题

**Q：打不开？**
1. 确认安全组/防火墙开了 3000 端口。
2. 看容器是否起来：`docker compose ps`（状态应是 Up）。
3. 看日志排错：`docker compose logs schola`。

**Q：想网址不带 `:3000`？**
编辑 `docker-compose.yml`，把 `ports` 那行改成 `"80:3000"`，然后 `docker compose up -d`。

**Q：怎么改代码后生效？**
代码用 git 拉的：`git pull` 然后 `bash update.sh`。
代码是 scp 传的：重新 scp 覆盖，再 `bash deploy.sh`。

---

## 进阶（可选）：绑域名 + HTTPS

免备案也能用域名，只要域名在**国外注册商**买（如 Cloudflare Registrar、NameSilo、Porkbun），解析到香港服务器 IP 即可，无需 ICP 备案。

想上 HTTPS（网址变 `https://`），最简单是在服务器上加一层 **Caddy** 反向代理自动申请证书——需要域名。等你确定要做了，我再帮你加 `Caddyfile` 和 compose 配置，这里先不展开，免得现在更复杂。

---

## 数据备份（重要）

数据库在 Docker 卷 `schola-data` 里。备份只需把库文件拷出来：

```bash
docker compose exec -T schola tar czf - -C /app data | cat > schola-backup-$(date +%F).tar.gz
```

要恢复就把这个包解压回服务器的卷挂载位置。改数据库结构（迁移）前，务必先备份一次。

---

## 长期维护

- 服务器重启 → 容器自动起来（`restart: unless-stopped`）。
- 更新代码 → `bash update.sh`。
- 真的完全不用管的概率很高，偶尔上来 `docker compose ps` 看一眼就行。
