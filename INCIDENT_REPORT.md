# 安全事件复盘报告 · 沙藏学馆（Schola Häagen-Dazs）

> **用途**：① 本次 OS 级沦陷事件的完整记录；② 后续同类事件的应急响应手册（AI 与人均可复用）。
> **时间**：2026-08-14 至 2026-08-16
> **定性**：宿主 OS root 被攻击者长期持有（真实后门 + 自愈），非单纯应用层问题。

---

## 0. 一句话结论

应用层的一个代码缺陷（`root` 列全员默认 = 1）让后台失去处置能力；攻击者借此（入口待查）一路提权到宿主 OS root，种下**每 60 秒自愈**的后门；因**零监控**，后门长期潜伏。处置标准动作不是「在脏机器里清理」，而是**快照 → 断网 → 从干净镜像重建 → 堵入口 → 多层设防 + 持续监控**。

---

## 1. 事件时间线

| 时间 | 事件 | 性质 |
|---|---|---|
| 08-14 | 用户发现「青衫」无端成初始管理员、封禁不持久、登录限流失效 | 症状（应用层） |
| 08-14~15 | 多次只读处置：解封误封账号、修中文主页 404、重置 QQ 密码、装 Cloudflare Tunnel 收口 HTTPS | 打补丁，但未触及根因 |
| 08-15 | 只读取证发现真实 OS 后门：`/opt/ops/sync.sh` + `schola-ops.service` + root crontab 互自愈 | **根因实锤** |
| 08-15 | 用户在 DMIT 控制台（带外）改 root 密码 + SSH 密钥，但后门每 60s 重塞攻击公钥，抵消 | 处置被后门抵消 |
| 08-15 | 取证连接被远程 `Connection closed`（疑似攻击者在场掐会话） | 对抗信号 |
| 08-15 深夜 | 用户**亲手关机** + 决定重装系统 | 正确处置：主动切断 |
| 08-15~16 | 准备加固：新部署密钥、localhost 绑定、非 root 容器、SSH 禁密码、检测脚本 | 重建准备 |
| 08-16 | 问责「为何疏忽／是否真做好」→ 升级检测为签名+行为双层，诚实标注未闭环缺口 | 复盘收敛 |

---

## 2. 失守根因（三层叠加）

### 2.1 地基级代码缺陷（我方责任）
- `users.root` 列历史默认值 = 1，且 `registerUser` 的 `INSERT` **不写该列** → 每个注册用户自动获得 `root=1`「免死金牌」。
- 后果：后台封禁 / 降级 / 删除对**任何人**失效，管理员被系统逻辑缴械。
- 修复：创始人归一化（仅 rector 一位 `root=1`）+ `registerUser` 显式写 `root=0` + 三处治理拦截改为「保护创始人 + 不可令 root 清零」。`commit 0774722`。

### 2.2 信任边界被击穿
- 应用本应被关在容器（沙盒）里，碰不到宿主机器。但 root 这类「上帝权限」一旦被滥用/泄露，等于在「应用层」与「宿主 root」之间凿了洞，攻击者顺洞提权到系统 root。
- 这是从「网站被黑」升级为「主机沦陷」的关键转折。

### 2.3 零监控导致潜伏
- 后门每 60 秒自检重生；当时**无任何告警**，手动查看时看着干净（自愈特性），故能长期潜伏不被发现。

---

## 3. 攻击者留下的痕迹（取证所得，速查）

- **攻击公钥指纹**（新系统上线后必须持续 grep 监控是否重现）：
  `ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIDERLxCN6MRokchXQNQPJhN8TsInhv56xYmRUdr7VpDM9 root@jefferey-dmit-hk`
- **后门机制**：`/opt/ops/sync.sh` 每 60s 把上述公钥重塞回 `/root/.ssh/authorized_keys`；`schola-ops.service` + root crontab 互自愈。
- **潜在触发器**：`.ops_ctl` 曾被怀疑为反向 shell 触发器（末次检查不存在，仍须警惕）。

---

## 4. 已实施的修复（含提交）

| 改动 | 文件 | 提交 |
|---|---|---|
| root 漏洞根治（归一化 + INSERT 写 root=0 + 治理拦截改写） | `lib/db.ts` `lib/actions.ts` | `0774722` |
| 应用只绑 `127.0.0.1` + 容器非 root 运行、不挂 docker.sock | `docker-compose.yml` `Dockerfile` | `191c2ad` |
| SSH 禁密码仅密钥、UFW 只开 22、Tunnel 收口 HTTPS | `deploy.sh` | `191c2ad` |
| 重装后恢复 + 加固清单 | `DEPLOY_RECOVER.md`（新增） | `191c2ad` |
| 入侵绊线（签名式）+ 主机一次性加固 + 纵深防御说明 | `scripts/monitor.sh` `scripts/harden-host.sh` `SECURITY_MONITORING.md`（新增） | `ee3f98b` |
| 绊线升级为**签名 + 行为双层**（完整性基线 / 异常端口 / 可疑路径进程 / 新增 SUID / 内核模块 / root 解释器） | `scripts/monitor.sh` | `d46eae5` |
| 新部署密钥（ed25519，私钥 gitignore，未入库） | `.deploy-keys/dmit_deploy_ed25519` | `191c2ad` |

> 当前所有加固配置已推上 `origin/main`。新系统 `git pull` 拉到的即关闭明文直连、非 root 运行、SSH 禁密码、Tunnel 收口 HTTPS 的版本。

---

## 5. 仍待闭环的缺口（最重要）

| 缺口 | 说明 | 责任 |
|---|---|---|
| 🔴 **入口未查明** | 攻击者怎么拿的 root（SSH 密钥泄露？Web RCE？还是 root 列 bug 接成的提权链？）至今没查清。不堵入口，新系统会被再来 | 新系统起来后查（离线 auth.log + `npm audit` + 审 `lib/actions.ts` 有无拼 shell） |
| 🔴 **真实主机尚未重建验证** | 脚本/配置已写好推仓库，但**还没在真机上跑过**；运行态未实测 | 按 `DEPLOY_RECOVER.md` 执行并反馈 |
| 🟠 **秘密轮换未实际执行** | DB 口令、Cloudflared 令牌、部署密钥、用户会话的**实际轮换**要在恢复时做（清 sessions + 重置口令 + 新隧道令牌） | 恢复流程里执行 |
| 🟠 **旧部署密钥可能仍暴露** | 旧 `github-actions-deploy` 公钥当时在受陷主机 authorized_keys；自动部署链路若仍用旧私钥需重生成 | 新系统重新生成部署密钥对 |

---

## 6. 反映的安全理念（可跨项目复用）

1. **最小权限** —— 谁都只给够用的权，root 不该人手一个（本次直接教训）。
2. **纵深防御** —— 一道墙不够，层叠设防，破一层还有层（网络 / 接入 / 运行 / 检测）。
3. **假设失守** —— 默认「已被攻破」来设计，而非信「固若金汤」。
4. **可检测性** —— 看不见就守不住；监控比围墙更早发现敌人（本次最缺失的一环）。
5. **不可变基线** —— 主机不可信时，从干净镜像重建 + 快照回滚，别修脏机器。
6. **快速隔离** —— 一发现沦陷先断网/关机，绝不在已沦陷主机内做清理（后门会自愈）。

---

## 7. 复用的应急响应手册（给自己）

遇到「疑似主机沦陷」时，按此顺序，**不要跳步**：

1. **第一假设 = 宿主机已 OS 级沦陷**。任何「网站异常」都要先排除这一层，不要在机器内做只读清理当终点。
2. **快照 + 断网隔离**。DMIT 控制台拍快照 → 关机/断网。把攻击者挡在「关机态」。
3. **干净重建**。从干净镜像 + git 干净代码重建；旧数据盘挂回只取 `schola.db`，拷前先停容器。
4. **查入口（最不能省）**。翻**离线** auth.log 找异常 IP/时间；`npm audit`；审 Server Actions 有无把用户输入拼进 shell/exec。不堵入口 = 白重装。
5. **多层设防**。应用只绑本地 + Tunnel 隐藏 IP + 非 root 容器 + SSH 仅密钥 + 防火墙最小开放。
6. **监控 + 离线兜底**。入侵绊线（签名+行为双层）+ DMIT 定时快照 + auth.log 转发独立日志机。rootkit 可隐藏本机进程/端口/日志，故离线兜底才是真底牌。

**签名检测局限**：只盯已知指纹（特定公钥/目录名），攻击者换马甲即失效 → 必须有**行为式**检测（异常进程/端口/文件改动/新内核模块）。行为式也只能缩短发现时间，非 100% 拦截。

---

## 8. 关键资产与指纹（速查）

- **生产主机**：`191.223.209.116`（DMIT 香港轻量节点，Ubuntu 22.04）
- **旧部署 SSH 私钥**：`/Users/jefferey/.ssh/dmit_key.pem`（Mac 本地，未上服务器，理论未泄露）
- **新部署密钥**：`.deploy-keys/dmit_deploy_ed25519`（ed25519，公钥已嵌 `deploy.sh` / `DEPLOY_RECOVER.md`，私钥 gitignore）
- **临时 Tunnel 地址**：`https://commerce-studied-hispanic-composite.trycloudflare.com`（quick tunnel，重启 cloudflared 会变；稳定地址需免费 CF 账号建 named tunnel）
- **攻击公钥指纹**：见 §3（上线后定期 `grep` 监控）

---

*本报告与 `SECURITY_MONITORING.md`、`DEPLOY_RECOVER.md` 互为配套。任何一次重建/恢复后，回来更新 §5 的缺口状态。*
